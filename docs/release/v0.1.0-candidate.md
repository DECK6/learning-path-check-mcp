# v0.1.0 candidate verification record

- Candidate date: 2026-07-13
- Service: `learning-path-check-mcp`
- MCP tools: exactly 10
- Runtime curriculum requests: no external network access

## Compiled data

| Item | Count |
|---|---:|
| Concepts | 7,506 |
| Topic concepts | 7,240 |
| Course concepts | 266 |
| Edges | 7,100 |
| Elementary prerequisites | 1,894 |
| Middle prerequisites | 2,155 |
| Supported high-school prerequisites | 2,932 |
| Elementary→middle prerequisites | 19 |
| Middle→high transitions | 50 |
| High course relations | 39 |
| Subject continuations | 11 |

High-school P0 excludes 528 vocational courses and 47,625 vocational topics. The full secondary source map retains those records; this MCP candidate intentionally does not compile them.

## SHA-256

```text
f1e54d47afc3d7e53a219ea5138c4358ac2f514e4870dab49963c80a1090a4c7  src/data/compiled/concepts.json
efa2da02c9d9252f84dcc10396d98afa5dad1a1b6340729b530ab42a83a970d8  src/data/compiled/edges.json
6061dd1443a692445ab567cb60f608c661f17b6551ab1d718343ce60cf754652  src/data/compiled/meta.json
8f7d070881e5483b8a931d2f18eda1a87245c1eb8cd18a450370b2aeef0fb6b7  src/data/compiled/search-index.json
73e5757f72069bdc1ef657e03cd8a49504aabdfd72231bde7511f396b503fbf8  bun.lock
```

## Verification contract

`bun run verify` runs TypeScript checking, the Bun test suite, and an independent production build. Tests cover graph reference integrity and DAG status, 7,240 topic standard-code mappings, memory/file store isolation and cascade deletion, trusted-header scope isolation, explicit guardian consent, anonymous write rejection, obvious-PII screening without input echo, production persistence/salt guards, deterministic search/trace behavior, the complete profile→check→assessment→plan→progress→report flow, MCP protocol initialization, property descriptions, annotations, request limits, and sanitized HTTP errors.

Final 2026-07-13 run:

- Secondary source map: 15 tests passed / 1,715 expectations; full SHACL Advanced graph (2,446,720 triples), 20 SPARQL queries, governance, manifests, ontology and UI checks passed.
- MCP: 29 tests passed / 21,619 expectations; the PostgreSQL live integration test was conditionally skipped because no `DATABASE_URL` was available.
- Production package: startup storage read/write self-test, `/health`, authenticated profile creation, file persistence, and missing-salt fail-closed startup passed; `/health` reported exactly 10 tools.

The packed production artifact was extracted into an empty temporary directory, installed with production dependencies only, started, and verified through `/health` with all 10 tools and four data versions. PostgreSQL integration is conditional on `DATABASE_URL`; schema and CRUD code type-check in every run.

The current workstation has no Docker-compatible CLI or daemon, so an OCI image build could not be executed locally. The Docker build's `bun install --frozen-lockfile` and `bun run build` stages were independently exercised, and the non-root/healthcheck Dockerfile is included for CI or a host with Docker. As of 2026-07-13, the repository owner confirmed public redistribution rights for the repository-authored compiled subset as documented in `NOTICE.md`; official curriculum publications remain outside the software license.

A private PlayMCP chat first confirmed that unauthenticated registration does not forward `x-playmcp-user-id`; state writes correctly failed closed. The release therefore uses PlayMCP `Key/Token 인증` with the custom `x-learning-path-token` header and requires a user-generated secret of at least 32 characters.

Private end-to-end verification then passed on KC server ID `3216` (`mcp-build-apply-3216`) at commit `4e41285`. PlayMCP forwarded the custom credential, and all ten tools succeeded across a realistic chain: curriculum search → overview → path trace → explicit guardian consent → profile create → check create → assessment → review plan → progress record → upcoming actions → parent report. The synthetic profile and its six linked records were cascade-deleted afterward. A separate KC stop → `Inactive` → start → `Active` probe preserved its synthetic profile in `/data`, after which that probe profile was also deleted. The PlayMCP entry remains private/temporary and `심사 전`; no review request or contest submission was made.
